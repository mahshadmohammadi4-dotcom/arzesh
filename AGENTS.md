# قوانین کار روی پروژه — تحلیل یار (v2)

این قوانین در **هر سشن** باید رعایت شوند. آن‌ها «قانون» هستند نه «خواهش».

## ۱. شروع سشن: همیشه وضعیت را بخوان
- اول `PROJECT-LOG.md` را بخوان (به‌خصوص ۱۰-۱۵ خط اول) تا بدانی کجاییم و چه بن‌بست‌هایی ثبت شده.
- اگر `PROJECT-LOG.md` قدیمی/خالی است، قبل از کار به‌روزش کن.

## ۲. ورودی جدید DEFT بالا
- هر تغییر جدید را **بالای** `PROJECT-LOG.md` اضافه کن (نه پایین)، تاریخ‌دار.
- ثبت کن: چه عوض شد، چرا، چه شکست (بن‌بست)، چه ماند.

## ۳. بن‌بست‌ها مهم‌ترند
- هر مسیری که رفتی و جواب نداد را فوراً در بخش «بن‌بست‌ها» ثبت کن تا سشن بعدی دوباره امتحانش نکند.

## ۴. مستندات قبل از کامیت (اجبار)
- ترتیب درست: ۱) تغییر کد → ۲) به‌روزرسانی `PROJECT-LOG.md` → ۳) `git add` → ۴) `git commit`.
- کامیت **هرگز** قبل از به‌روزرسانی مستندات نزن.

## ۵. فازهای شماره‌دار
- کار در قالب فازهای شماره‌دار انجام می‌شود (مرجع: `DEVELOPMENT-PLAN.md`).
- هر سشن فقط «فاز X را اجرا کن»؛ نقشه‌کشی بزرگ فقط در سشنِ طراحی جداگانه.

## ۶. اصول محصول (فراموش نشود)
- **داده فقط از اسناد/حسابداری/API** می‌آید؛ هیچ صفحه‌ی «ورود دستی» یا «صفحه گسترده مادر».
- نشانگر وضعیت: 🔵 = تغییر کاربر، 🔴 = تعارض، بدون‌نقطه = داده اصلی.
- پرسشنامه (توصیف+تأیید) مخصوص **امکان‌سنجی** است، نه تحلیل کسب‌وکار.
- هر قابلیت: با کلیک کاربر، منابع + اطمینان + تاریخچه دیده شود.
- مدیر باید بتواند قفل کند (قابل‌ویرایش/فقط‌خواندنی بودن هر منبع داده).

## ۷. سالم‌سازی هر سشن
- پایان هر سشن: `git status` تمیز + `git commit` + `git push`.
- بعد از هر تغییر فایل config (opencode.json / agent / plugin): به کاربر بگو **quit و restart opencode** بزند.

## ۸. راستی‌آزمایی
- ادعاها را با کد/اجرا چک کن، نه با حافظه. اگر نمی‌توانی اجرا کنی (مثلاً node در این محیط نیست)، شفاف بگو و فقط از `curl`/بررسی استاتیک مطمئن شو.
