# نقشه راه توسعه — تحلیل یار (فاز تکامل پروتوتایپ)

> سند مرجع برآمده از: طرح توسعه اولیه + فیدبک جلسه با مشتری + تحلیل تشابه با Fathom و COMFAR
> هدف: ادامه‌ی بهبود پروتوتایپ فعلی بدون بازسازی از صفر

---

## ۱. خلاصه محصول

پلتفرم یکپارچه تحلیل مالی با سه محصول اصلی:

| سرویس | استراتژی |
|--------|----------|
| تحلیل کسب‌وکار (Business Analysis) | حفظ و بهبود |
| امکان‌سنجی (Feasibility Study) | حفظ و بهبود |
| ارزش‌گذاری (Valuation) | حفظ و بهبود |

**تمایز از رقبا:**
- مشابه Fathom (اتصال داده حسابداری + تجسم مالی + تحلیل تاریخی) **به‌علاوه** قابلیت‌های هوشمند: دستیار AI، ویرایش کنترل‌شده، تشخیص تعارض، ردیابی منبع، تصحیح انسانی، تولید تحلیل.
- مشابه COMFAR (قدرت مدل‌سازی) اما **کاربرپذیر** برای کاربر عادی.

---

## ۲. قواعد توسعه (قوانین طلایی)

**ممنوع:**
- ساخت اپلیکیشن یا دیتابیس جدا
- شکستن قابلیت‌های موجود تحلیل کسب‌وکار / ارزش‌گذاری
- حذف ویژگی کارکردی فعلی
- جایگزینی معماری بدون دلیل

**همیشه:**
- استفاده مجدد از کامپوننت‌ها، CSS، تم، ناوبری، ساختار پروژه، سیستم KPI و زیرساخت گزارش

---

## ۳. فلسفه داده (بسیار مهم — طبق فیدبک مشتری)

### ۳.۱. ورود داده = اسناد، نه فرم
- **صفحه «ورود دستی داده» حذف می‌شود** (Master Sheet)
- ورود اطلاعات فقط از: اسناد بارگذاری‌شده، OCR، سیستم حسابداری، منابع خارجی
- کاربر در حین کار، داخل صفحات تحلیل، داده را می‌بیند و در صورت نیاز اصلاح می‌کند

### ۳.۲. مدل «یک نقطه» — نمایش وضعیت هر داده
هر دیتاپوینت، وضعیت مشخص دارد:

| وضعیت | نمایش | عملکرد کاربر |
|-------|-------|--------------|
| داده اصلی (حسابداری/OCR/منبع خارجی) | بدون نشانگر | فقط نمایش |
| تغییر توسط کاربر (اصلاح) | **نقطه آبی** | کلیک → مشاهده مقدار اصلی، مقدار جدید، منبع، تاریخچه تغییرات |
| تعارض بین منابع (Conflict) | **نقطه قرمز** | کلیک → رسیدگی و انتخاب مقدار / اصلاح |

- کاربر اجبار به تأیید تکی ندارد؛ فقط اگر چیزی اصلاح کرد یا تعارضی بود، نشانگر ظاهر می‌شود.
- این مدل جایگزین هرگونه «تأیید/رد تکی» می‌شود.

### ۳.۳. مدل فاتوم + کنترل انسانی
- فاتوم داده را می‌گیرد ولی **کاربر نمی‌تواند تغییر دهد**.
- ما: اتصال را نگه می‌داریم **به‌علاوه** امکان تغییر کنترل‌شده.
- **مدیر باید بتواند قفل کند** هر منبع داده را (هیچ‌کس نباید بتواند تغییر دهد):
  - داده واردشده از سیستم حسابداری: قفل / قابل ویرایش
  - داده استخراج‌شده OCR: قفل / قابل ویرایش

---

## ۴. نقشه راه فازبندی (تلفیق هر دو سند)

### اسپرینت ۱ — تحلیل کسب‌وکار: زیرساخت داده و پرونده شرکت
- [ ] حذف صفحه گسترده مادر (Master Sheet) از نمایش — داده داخل تحلیل/شاخص‌ها دیده و قابل تغییر باشد
- [ ] سیستم نشانگر وضعیت داده: نقطه آبی (تغییر کاربر) / نقطه قرمز (تعارض)
- [ ] نمایش منبع + درصد اطمینان روی هر داده (در کارت‌های اطلاعات شرکت)
- [ ] پرونده شرکت: حفظ کارت‌ها + تغییر چیدمان طبق نظر مشتری + صفحه **جداگانه چارت سازمانی**
- [ ] قفل/قابل‌ویرایش بودن هر منبع داده توسط مدیر (تنظیمات حساب)

### اسپرینت ۲ — تحلیل کسب‌وکار: داده مالی و پیش‌بینی
- [ ] ساختار درختی (expandable tree) برای صورت‌های مالی —
  صورت مالی → درآمد → درآمد محصول → جزئیات، بدون نمایش همه چیز یک‌باره
- [ ] پیش‌بینی تعاملی: پیش‌فرض = دید تاریخی؛ دکمه «نمایش پیش‌بینی» → اسلایدر + کنترل‌های سناریو + ابزار پیش‌بینی؛ بدون اجباری دیدن ابزارها در ابتدا

### اسپرینت ۳ — تحلیل کسب‌وکار: ایجنت، نسبت‌ها، ریسک
- [ ] **ایجنت در صفحه** (نه چت‌بات جدا):
  - توضیح با جزئیات («این نسبت یعنی چی؟»)
  - اعمال تغییرات در بخش‌های تاییدشده («این مفروضه را تغییر بده»)
  - تولید تحلیل
- [ ] نرخ نسبت‌های مالی: **اول جدول کامل** (نسبت × سال۱ × سال۲ × سال۳) → کلیک → روند تفصیلی
- [ ] تحلیل ریسک: هر ریسک = توضیح + تحلیل با پرامپت هوش مصنوعی + پشتیبانی جدول/ماتریس/دسته‌بندی
- [ ] صفحه خروجی: مورد تأیید «خانم محمودی» برای نحوه نمایش (شبیه Fathom)

### اسپرینت ۴ — تحلیل کسب‌وکار: اخبار و ایجنت
- [ ] **صفحه ادمین برای مدیریت پرامپت‌ها**: تعریف پرامپت اولیه / جستجو / تحلیل / کوئری‌های آماده
  - مثال: پرامپت ریسک، پرامپت اخبار، پرامپت تحلیل بازار
- [ ] ایجنت اجراکننده پرامپت برای پایش و تحلیل اخبار
- [ ] نمایش این تحلیل‌ها با «تحلیل خوب» برای کاربر

### اسپرینت ۵ — امکان‌سنجی: پرسشنامه پایه هوشمند
- [ ] فلو جدید: بارگذاری سند → توصیف آزاد مشتری (مثل پرامپت GPT) → درک سیستم → **صفحه «نگاه سیستم»** (System Understanding) → قابلیت ویرایش/تأیید/ادامه
- [ ] پرسشنامه فعلی به صفحه دوم تبدیل شود (قابل مشاهده و ویرایش)
- [ ] پس از عبور از پرسشنامه: **اخطار «کد اعتبار مصرف می‌شود»** → ایجاد پروژه امکان‌سنجی؛ برای طرح بعدی باید پروژه جدید ساخت

### اسپرینت ۶ — امکان‌سنجی: سرمایه‌گذاری ثابت
- [ ] سیستم بر اساس پرسشنامه، جداول را **خودکار می‌سازد** و نمایش می‌دهد
- [ ] کاربر: تأیید کلی/رد، ویرایش، حذف جدول، افزودن جدول، ساخت جدول سفارشی (مثل «تجهیزات حفاری»: نام ستون‌ها، سطرها، مقادیر)
- [ ] نشانه‌گذاری تغییرات با رنگ (بدون تأیید ردیف به ردیف؛ تأیید در انتهای هر جدول)
- [ ] ظاهر جدول‌ها ساده، تمیز، شبیه اکسل با پولیش بالا (COMFAR وایب اما ساده‌تر)

### اسپرینت ۷ — امکان‌سنجی: هزینه‌های تولید و فروش
- [ ] هزینه‌های تولید: دو حالت **فرایندمحور** (مواد/دستمزد/سربار تولید…) و **پروژه‌محور** (مواد پروژه/نیرو/پیمانکار…) — تغییرپذیر توسط کاربر
- [ ] فروش: جدول‌های منعطف — افزودن محصول، ردیف، توسعه ساختار
- [ ] **فلو ویزارد**: هر صفحه → Previous / Next تا صفحه نهایی

### اسپرینت ۸ — امکان‌سنجی: گزارش و ویرایش با ایجنت
- [ ] قالب گزارش (الگو): بانکی / مدیریتی / جامع — انتخاب در پرسشنامه (هدف گزارش) → فهرست متناسب‌سازی شود
- [ ] فهرست (Outline): افزودن/حذف/جابجایی/تبدیل به زیرمجموعه/سلسله‌مراتب/مرتب‌سازی مجدد (با تیک‌زدن)
- [ ] ویرایش متن نهایی در محل (مثل Fathom) + ایجنت: بازنویسی/خلاصه‌سازی/توسعه توضیحات/خلاصه مدیریتی/تنظیم لحن
- [ ] تم‌های گزارش برای خروجی

### اسپرینت ۹ — ارزش‌گذاری (پولیش)
- [ ] حفظ جداول و توضیحات، لینک‌گذاری متنوع‌تر
- [ ] سایدبارهای کنترل ارزش (مشابه صفحه ارزش‌گذاری Fathom)
- [ ] بررسی الگوی نمایش «مارجین ایمنی» (Safety Margin)

### اسپرینت ۱۰ — رگرشن و ارائه
- [ ] تست کامل همه ماژول‌ها (BA / FS / Valuation)
- [ ] پولیش UI + آماده‌سازی دموی مشتری

---

## ۵. قواعد تست (غیرقابل چشم‌پوشی)

بعد از هر فاز:

**تحلیل کسب‌وکار:** باز می‌شود؟ صفحات مالی کار می‌کنند؟ داشبورد کار می‌کند؟ ارزش‌گذاری سالم است؟

**امکان‌سنجی:** پرسشنامه کار می‌کند؟ جدول‌ها محاسبات را به‌روز می‌کنند؟ داشبورد به‌روز می‌شود؟

هیچ فازی بدون رگرشن کامل نمی‌شود.

---

## ۶. کار با گیت‌هاب

- ریپوی جدید برای پروژه ساخته می‌شود (این پوشه کنونی: `final-v1/ارسال برای مشتری/`)
- هر فاز از `DEVELOPMENT-PLAN.md` = یک یا چند کامیت با message مشخص (مثال: `feat(ba): remove master sheet + data-state indicators`)
- فایل `DEVELOPMENT-PLAN.md` در ریشه پروژه نگهداری می‌شود و مبنای رهگیری است.
- تست رگرشن قبل از هر کامیت فاز.

---

## ۷. خروجی نهایی (Vision)

پلتفرم هوشمند امکان‌سنجی و تحلیل کسب‌وکار شامل: تحلیل مالی + دستیار AI + هوش اسناد + ویرایش کنترل‌شده + مدل‌های پویا + گزارش‌دهی حرفه‌ای.
سادگی SaaSهای مدرن + قدرت ابزارهای حرفه‌ای مثل COMFAR و Fathom.
