# دفتر خاطرات پروژه — تحلیل یار (v2)

> **قانون:** ورودی جدید همیشه **بالای** فایل. هر سشن اول این فایل را بخوان تا بدانی «کجاییم». اینجا هم موفقیتها و هم بن‌بست‌ها ثبت می‌شوند.

---

## ۱۴۰۵/۰۶/۱۴ — هزینه‌های تولید: تأیید کلی + نشانگر ویرایش (الگوی نهایی)
- چه کردیم: حذف تأیید تکی؛ دکمه «✔ تأیید کل جدول» + نشانگر ویرایش (نقطه/رنگ) برای ردیف‌های ادیت‌شده. پیشنهاد سیستم/تأییدشده هم حالت دارند. الگوی سرمایه‌گذاری ثابت تعمیم یافت.

---

## ۱۴۰۵/۰۶/۱۴ — سرمایه‌گذاری ثابت: نشانگرِ ویرایش‌شده + رنگ وضعیت
- چه کردیم: با ادیت/افزودن مقدار، ردیف edited و unconfirmed می‌شود؛ نقطه آبی + رنگ متمایز. پیشنهاد سیستم و تأییدشده هم رنگ و نشانگر دارند.
- status: کامل (الگوی نهایی). بعدی: تعمیم به هزینه‌ها/سرمایه در گردش/تأمین مالی.

---

## ۱۴۰۵/۰۶/۱۴ — سرمایه‌گذاری ثابت: تأییدِ کلی + رنگ وضعیت (حذف تأیید تکی)
- چه کردیم: تأییدِ ردیف‌به‌ردیف حذف شد؛ دکمه «✔ تأیید کل جدول» (جمعی) در انتهای هر جدول + راهنمای رنگ (پیشنهاد/ویرایش‌شده/تأییدشده). جدول سفارشی و افزودن/حذف حفظ شد.

---

## ۱۴۰۵/۰۶/۱۴ — پرسشنامه پایه: هشدار سهمیه بعد از تکمیل + پیش‌پرِ ورود/ثبت
- چه کردیم: هشدارِ «مصرف سهمیه/ایجاد طرح» از نگاه سیستم به هنگام عبور از «پرسشنامه پایه» (بعد از تکمیل اطلاعات) منتقل شد (یک‌بار).
- ورود و فرم ثبتِ اطلاعات شرکت از پیش پُر شدند (فقط با تأیید جلو می‌رود).
- status: کامل.

---

## ۱۴۰۵/۰۶/۱۴ — فاز FS-4: ستون منعطف + گارد در «هزینه‌های تولید»
- چه کردیم: مکانیزم عمومی fsX* برای جدول‌های خطی؛ ستون اضافی (افزودن/حذف/ویرایش سلول) + گاردِ تغییرِ ستون‌های محاسباتی روی «هزینه‌های تولید» (ساده + نیروی کار). ردیف‌ها قابل افزودن/حذف.
- status: کامل (پیش‌نویس). بعدی: جدول سفارشی در هزینه‌ها + تعمیم fsX به سرمایه در گردش و تأمین مالی.

---

## ۱۴۰۵/۰۶/۱۴ — فاز FS-4: ستون/سطر منعطف + گارد تعارض در جدول‌های سرمایه‌گذاری ثابت
- چه کردیم: جدول‌های پیش‌فرضِ سرمایه‌گذاری ثابت حالا «افزودن/حذف/ویرایش ستون» + «افرودن/حذف ردیف» دارند؛ گارد تعارض مانع حذفِ ستون‌های محاسباتی یا نام تکراری می‌شود (با پیام دلیل). دکمه «✎ ویرایش ستون‌ها» برای تعریف/تایید ستون‌ها.
- رفع باگ: دسترسی به داده از `d[cat]` اصلاح شد (قبلاً `d.fixedInvestment[cat]` اشتباه بود).
- status: کامل. مرحله بعدی: تعمیم به سایر جدول‌ها.

---

## ۱۴۰۵/۰۶/۱۴ — فاز FS-4: جدول سفارشیِ منعطف (ستون/سطر/مقادیر آزاد)
- چه کردیم: جدول سفارشی در سرمایه‌گذاری ثابت به یک «برگه‌ی انعطاف‌پذیر» تبدیل شد (افزودن/حذف ستون و سطر، ویرایش سلول‌ها، عنوان قابل‌ویرایش، حذف جدول) — مطابق خواسته کارفرما «ساخت جدول سفارشی».
- status: کامل (پیش‌نویس). مرحله‌ی بعدی: انعطاف جدول‌های پیش‌فرضِ سیستم.

---

## ۱۴۰۵/۰۶/۱۴ — فاز FS-3 UX: هماهنگی با پنل کناری + پرش به گزارش
- چه کردیم: وقتی کاربر به تب کناری/غیرمرحله‌ای (اسناد/نمای کلی/برنامه/دسترسی) می‌رود، نوار پایین «برگشت به جریان امکان‌سنجی» را نشان می‌دهد و با کلیک به آخرین مرحله (fsWizStep) برمی‌گردد. دکمه «پرش به گزارش ⤒» نیز اضافه شد.
- status: کامل.

---

## ۱۴۰۵/۰۶/۱۴ — فاز FS-3 (تکمیل): تأیید→بعدی + قدمزدنِ ایتمبهایتم درون صفحه
- چه کردیم: دکمه «تأیید و ادامه» هر مرحله را ثبت (✓) و به مرحله بعد میبرد. و «بعدی» حالا **داخل همان صفحه ایتمبهایتم** از بالا به پایین میگردد (اسکرول + هایلایت طلایی) و فقط پس از تمام شدن ایتمها به مرحلهی بعد میرود (طبق خواسته: بدون عوض شدن کل صفحه با هر کلیک).
- status: کامل. مرحلهی بعدی: FS-4 (جداول پویا).

---

## ۱۴۰۵/۰۶/۱۴ — فاز FS-3: ویزارد مراحلی (استپر قابلیکلیک + نوار پایین چسبان)
- چه کردیم: استپر ۸ مرحلهای (پرسشنامه→سرمایه ثابت→هزینهها→فروش→سرمایه در گردش→تأمین مالی→نتایج→گزارش) قابلیکلیک + نوار عملیات چسبان در پایین صفحه (رفع بنبست) با جهت RTL درست (بعدی راست، قبلی چپ).
- چرا: مطابق فیدبک کارفرما «نکستنکست + امکان پرش/ادیت از هر جا + نرسیدن به بنبست».
- وضعیت: پیشنویس نحوهی نمایش؛ منتظر بازبینی UX.

---

## ۱۴۰۵/۰۶/۱۴ — فاز FS-2: نگاه سیستم (درک هوشمند + پیشپُر شدن)
- چه کردیم: پنل «نگاه سیستم» در بالای پرسشنامه‌ی امکان‌سنجی: نمایش توصیف طرح + فیلدهای تفسیرشده (حوزه، مقیاس، سرمایه‌گذاری، نرخ تنزیل) قابل ویرایش + دکمه «تأیید و ادامه» که جدول‌های طرح را پیش‌پُر می‌کند (تغذیه از `aiFillDetailQuestionnaire`).
- چرا: مطابق فیدبک کارفرما «کوئری → درک سیستم → تأیید تکی → ادامه».
- وضعیت: کامل (شبیه‌سازی؛ در نسخه عملیاتی NLP واقعی).

---

## ۱۴۰۵/۰۶/۱۴ — فاز FS-1: ورودیِ «توضیح طرح (کوئری)» برای امکان‌سنجی
- چه کردیم: در ویزارد ایجاد پروژه‌ی امکان‌سنجی، فیلد «توضیح طرح» (textarea، مثل پرامپت) اضافه شد؛ ذخیره روی پروژه (`feasibilityDescription`).
- چرا: مطابق فیدبک کارفرما «صفحه اول کوئری/توصیف از کاربر، سپس پر شدن بخش‌ها و تأیید تکی».
- وضعیت: کامل. مرحله‌ی بعدی: FS-2 (درک هوشمند + پیش‌پُر شدن).

---

## ۱۴۰۵/۰۶/۱۴ — ارزش‌گذاری: متریک‌های کلیدی + حاشیه اطمینان + زیرساخت سرور
- چه کردیم:
  - صفحه «ارزش‌گذاری»: بخش «شاخص‌های کلیدی» (کارت: EV، ارزش حقوق، بدهی خالص، ROE، حاشیه‌ها، بدهی/حقوق، ریسک) + کارت «حاشیه اطمینان» (seal + verdict بر اساس افت بدبینانه؛ هم‌ساختار با بقیه صفحات).
  - زیرساخت سرور: `serve.py` با هدر no-cache (رفع مشکل کشِ مرورگر) + به‌روزرسانی `serve.sh`. مخفی‌سازی اسکرول‌بار سفید نوار کناری.
  - (نکته: قابلیت «ویرایش داده‌های مالی» که زیردرخت را می‌شکست، کنار گذاشته شد؛ صفحه به نسخه‌ی سالم بازگشت.)
- چرا: مطابق فیدبک مشتری و الگوی Safety Margin، با حفظ سازگاری ساختاری.
- وضعیت: کامل (نمایش پیشنهادی؛ منتظر بازبینی).

---

## ۱۴۰۵/۰۶/۱۴ — ماژول تحلیل کسب‌وکار: ایجنت درون‌صفحه (به‌جای چت‌بات)
- چه کردیم: چت‌بات گفت‌وگویی حذف شد؛ به‌جای آن یک پنل ایجنت درون‌صفحه با دو کار:
  - «توضیح دقیق» → پنل توضیحِ بخش فعال (نسبت‌ها/ریسک/صورت مالی).
  - «اعمال تغییر مفروض» → فرم ویرایش مفروضات پیش‌بینی (تورم/رشد فروش/رشد تولید) → به‌روزرسانی پیش‌بینی.
- چرا: مطابق فیدبک مشتری (چت‌بات نه؛ ایجنت با دو کار). نمایش پیشنهادی است و منتظر نظر خانم محمودی.
- وضعیت: کامل (پیش‌نویس نمایش).

---

## ۱۴۰۵/۰۶/۱۴ — ماژول تحلیل کسب‌وکار: چند-انتخابی/تحلیل هوشمند ریسک (شیت 5)
- چه کردیم: در بخش تحلیل ریسک (که از قبل طبقه‌بندی استاندارد + ماتریس احتمال×اثر + توصیه‌ها داشت)، چک‌باکس «تحلیل هوشمند» روی هر کارت ریسک + دکمه «تحلیل هوشمند N طبقه» + پنل توضیح/امتیاز/توصیه برای انتخاب‌شده‌ها اضافه شد.
- منبع: شیت «5 تحلیل» — تحلیل ریسک به تفکیک طبقه‌بندی استاندارد + تحلیل هوشمند.
- وضعیت: کامل. تحلیل‌ها نمونه؛ در نسخه عملیاتی از مدل AI.

---

## ۱۴۰۵/۰۶/۱۴ — ماژول تحلیل کسب‌وکار: جدول نسبت‌ها + چند-انتخابی/AI (شیت 5 و 5-1)
- چه کردیم:
  - جدول نسبت‌های مالی به‌صورت «جدول اول → کلیک روند» (الگوی فاتوم/Syft/اسپات‌لایت).
  - گسترش به ۴ گروه ماکرو با فهرست کامل شیت 5-1: اهرم/ساختار+پوشش بهره، کارایی/نقدینگی، سودآوری/بازدهی، جریان نقد/بازار و سهام (~۲۸ نسبت).
  - چند-انتخابی (checkbox) + دکمه «تحلیل هوشمند» → پنل تحلیل AI برای نسبت‌های انتخابی.
  - برای نسبت‌های بدون داده در مدل، مقدار نمونه؛ قابل محاسبه‌ها از شیت محاسبه می‌شوند.
- منبع: فیدبک مشتری + شیت «5 تحلیل» و «5-1 نسبت‌های مالی» + بررسی سایت‌های مشابه.
- وضعیت: کامل. اعداد نمونه؛ نمایش را مشتری/خانم محمودی بازبینی می‌کند.

---

## ۱۴۰۵/۰۶/۱۴ — پرونده شرکت: منبع/اطمینان فقط با کلیک
- چه کردیم: نمایش درون‌خطی «منبع» و «اطمینان ٪» از پرونده شرکت حذف شد. فقط نقطه رنگی (آبی/قرمز) + راهنمای «برای جزئیات کلیک کنید» ماند.
- چرا: مطابق تصمیم مشتری؛ اطلاعات پنهانِ درون‌خطی نیست، بلکه با کلیک (پاپ‌آپ: منبع + اطمینان + تاریخچه) دیده می‌شود. داده فقط با نمایش/کلیک.
- وضعیت: کامل.

---

## ۱۴۰۵/۰۶/۱۴ — اسپرینت ۲ (بخش ۱): پیش‌بینی تعاملی + ساختار واقعی صورت‌ها
- چه کردیم:
  - پیش‌فرض پیش‌بینی «خاموش» شد (پیش‌فرض = دید تاریخی). دکمه «📈 نمایش پیش‌بینی» → اسلایدر/سناریو ظاهر می‌شود؛ «✕ بستن پیش‌بینی» برمی‌گردد.
  - ساختار واقعی هر چهار صورت (سود و زیان / ترازنامه / جریان وجوه نقد / تغییرات حقوق مالکانه) از روی تصاویر اکسل مشتری استخراج و در درخت (`BA_FINANCIAL_DEMO` + `BA_FIN_DRILLDOWN`) بازسازی شد.
  - آیتم‌های واقعی: سود/زیان (سود قبل از کسر مالیات + مالیات بر درآمد)، ترازنامه (زیردرخت حقوق صاحبان سهام)، جریان وجوه نقد (۳ فعالیت + تغییرات موجودی نقد)، حقوق مالکانه (ردیف‌های واقعی).
  - اعداد: طبق تصمیم مشتری نمونه‌اند (مهم ساختار/آیتم‌هاست، نه اعداد دقیق).
- نکته: پنل پیش‌بینی و ستون‌های پیش‌بینی با `BA_FIN_UI.forecast` گیت شده‌اند و از قبل درست بودند؛ فقط پیش‌فرض روشن بود که خاموش شد.

---

## ۱۴۰۵/۰۶/۱۴ — زیرساخت حافظه‌ی پروژه نصب شد
- چه کردیم: زیرساخت «حافظه‌ی ماندگار» (این فایل + AGENTS.md + opencode.json) ساخته شد تا هر سشن از آخرین وضعیت شروع کند، نه از صفر.
- چرا: مشکلِ «هر سشن از نو شروع می‌کنی» باید یک‌بار برای همیشه حل شود.
- وضعیت: فعال. هر سشن جدید باید این فایل را بخواند و قوانین AGENTS.md را رعایت کند.

## ۱۴۰۵/۰۶/۱۴ — اسپرینت ۱ (تحلیل کسب‌وکار) تکمیل شد
- چه کردیم:
  - زیرساخت نشانگر وضعیت داده (computeFieldState): `original / modified / conflict`
  - نشانگر رنگی: 🔵 تغییر کاربر، 🔴 تعارض، بدون‌نقطه = داده اصلی
  - پاپ‌آپ کلیک‌پذیر «وضعیت فیلد»: منابع + اطمینان + تاریخچه + انتخاب/ویرایش
  - پرونده شرکت: ردیف‌ها کلیک‌پذیر، چارت سازمانی صفحه جدا، ویرایش هیئت‌مدیره/سهامداران/HR
  - حذف «صفحه گسترده مادر» و «پرسشنامه هوشمند BA/ارزش‌گذاری» → داده فقط از اسناد («گردآوری اطلاعات»)
- نکته: پرسشنامه‌ی امکان‌سنجی حفظ شد (فلوی توصیف+تأیید مخصوص FS است، نه BA).
- نتیجه: کاربر داده را داخل تحلیل/شاخص/پرونده می‌بیند، با کلیک رسیدگی/ویرایش می‌کند؛ صفحه ورود دستی حذف شد.

## بن‌بست‌ها (Dead-ends) — دیگر تکرار نشوند
- **node در این محیط نصب نیست** (`node`/`deno`/`bun`/`qjs` هیچ‌کدام موجود نیست) → نمی‌توان JS را مستقیم اجرا/تست کرد؛ فقط استاتیک از طریق `curl` بررسی می‌شود.
- **سرور زنده‌ی `python3 -m http.server` وقتی شل تایم‌اوت می‌شود، پروسه‌اش کُشته می‌شود.** راه‌حل: اجرا با `setsid ... &` و `disown` تا از شل جدا بماند. پورت: `8900`.
- **جدا کردن توکن از URL ریموت کافی نیست**؛ فرمت `~/.git-credentials` باید `user:token@github.com` باشد (نه فقط `token@`)، وگرنه push خطای `could not read Username` می‌دهد.
- **هویت گیت تنظیم نبود** → با `git config user.name/email` در سطح ریپو تنظیم شد.
- **`git init -b main` پشتیبانی نمی‌شود** (نسخه‌ی قدیمی) → `git init` سپس `git branch -m main`.

## قواعد ثابت (خلاصه — متن کامل در AGENTS.md)
- داده فقط از اسناد/حسابداری/API؛ هیچ صفحه‌ی ورود دستی.
- نشانگر: آبی=تغییر کاربر، قرمز=تعارض، هیچ=اصلی.
- قبل از هر کامیت، این فایل و مستندات به‌روز شوند؛ کامیت = آخرین قدم.
- هر سشن = یک یا چند فاز شماره‌دار؛ فقط بگو «فاز X را اجرا کن».

## جاهایی که هستیم — وضعیت فعلی
- شاخه: `main`، ریپو: `mahshadmohammadi4-dotcom/arzesh`
- کامیت‌ها: `f4b615e` (پایه v2) → `7267078` (نشانگر) → `6b58726` (نشانگر نمای کلی/شاخص) → `cbacf8e` (پاپ‌آپ) → `383c683` (پرونده شرکت) → `8f41da2` (حذف مستر/پرسشنامه)
- **گام بعدی (اسپرینت ۲):** داده‌های مالی + پیش‌بینی تعاملی (ساختار درختی صورت‌ها؛ «نمایش پیش‌بینی» → اسلایدر/سناریو، پیش‌فرض دید تاریخی).

## سوالات باز / تصمیم‌های عقب‌افتاده
- آیا «پرسشنامه» در ویزارد ایجاد پروژه‌ی BA هم باید حذف شود؟ (فعلاً ایجاد پروژه بعد از ساخت به صفحه «گردآوری اطلاعات» می‌رود.)
